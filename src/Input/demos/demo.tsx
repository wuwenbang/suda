import React from 'react';
import { Input } from 'suda';

const Demo = () => {
  return (
    <div style={{ display: 'flex', gap: 12 }}>
      <Input placeholder="请输入内容" />
    </div>
  );
};

export default Demo;
